import React from "react";

const Total = (props) => {
    return (
        <div>
            <p>Number of exercises {props.total.parts[0].exercises
            + props.total.parts[0].exercises
            + props.total.parts[0].exercises}</p>
        </div>
    )
}

export default Total